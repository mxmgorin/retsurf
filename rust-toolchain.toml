[toolchain]
# Newest stable: the hot code is Rust, so rustc's LLVM is what moves performance.
# Above the fork's 1.97.1 pin; egui 0.36 needs >= 1.95.
channel = "1.98.0"

components = [
    "clippy",
    "rustfmt",
    # For rust-analyzer
    "rust-src",
]
